apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    appname: busybox-cephfs
  name: dd-io-pvc-6
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 129Gi
  storageClassName: ocs-storagecluster-cephfs
